// ===================================================================
// スマホ対応(レスポンシブ)で使うブレークポイントの定数。
// 「スマホ対応できるように大改良してほしい」というご要望を受けて追加した。
// このアプリは元々CSSフレームワークやCSS-in-JSライブラリを使わず、
// makeStyles(theme) が返すプレーンなJSのstyleオブジェクトで見た目を
// 組み立てる方式(roomShapes.js等と同じ、src直下のユーティリティファイルの
// 慣習)を取っているため、ここでは「@media」クエリではなく、単純な
// ピクセル幅のしきい値だけを1箇所にまとめておき、各ページはこの値を
// 使ってJS側で(hooks/useViewport.js経由で)レイアウトを分岐させる。
// ===================================================================

// この幅未満をスマートフォン相当として扱う(iPhone/Androidの主要機種は
// 縦向きでおおよそ360〜430px程度、タブレットは768px以上が多いため、
// 一般的な目安である768pxを採用している)。
export const MOBILE_BREAKPOINT_PX = 768;
